{
  "name": "timbre",
  "version": "0.16.1",
  "private": true,
  "description": "One search across free music. Nothing stored, nothing hosted.",
  "scripts": {
    "dev": "pnpm --filter @timbre/web dev",
    "dev:ytmusic": "node scripts/free-port.mts 8787 && dotenv -e .env -- apps/ytmusic/.venv/Scripts/python -m uvicorn app.main:app --reload --port 8787 --app-dir apps/ytmusic",
    "dev:ytmusic:posix": "node scripts/free-port.mts 8787 && dotenv -e .env -- apps/ytmusic/.venv/bin/python -m uvicorn app.main:app --reload --port 8787 --app-dir apps/ytmusic",
    "build": "pnpm --filter @timbre/web build",
    "start": "pnpm --filter @timbre/web start",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck && tsc -p scripts",
    "typecheck:fast": "pnpm -r --parallel typecheck:fast && tsgo -p scripts",
    "test": "pnpm -r --parallel test && node --test \"scripts/*.test.mts\"",
    "spotify:canary": "node scripts/spotify-canary.mts"
  },
  "engines": {
    "node": ">=22.18"
  },
  "packageManager": "pnpm@11.11.0",
  "devDependencies": {
    "@types/node": "^20",
    "@typescript/native-preview": "7.0.0-dev.20260707.2",
    "dotenv-cli": "^11.0.0",
    "typescript": "^5"
  }
}
